import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Protected - Editable',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
